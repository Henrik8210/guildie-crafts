**CurseForge packaging fixes** — `.pkgmeta` aligned with official docs (no BOM, simple `manual-changelog`), `## X-Curse-Project-ID: 1655417` in the TOC, debug code excluded from releases, and GitHub Actions upload on tag push.

Same addon features as v2.2.3 (icon color fix, notifications, order status bands).
